import { Interface } from "readline"

export interface IBooks {
    id: number,
    title: string,
    desc: string,
    author: string,
    ISBN: string,
    price: number,
    pages: string,
    image: string
}